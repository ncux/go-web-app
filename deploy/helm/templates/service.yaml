apiVersion: v1
kind: Service
metadata:
  name: go-web-app-service
  namespace: web-apps
spec:
  selector:
    app: go-web-app
  type: NodePort
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http